# CLAUDE CURRENT STATE AUDIT

> Generated 2026-08-25. Source of truth: real clone of `main` (commit `ff7c437`,
> parent tag `v0.8.0` at `302cdee`), a fresh `pip install -e ".[dev]"` +
> `npm install`, and actual `pytest` / `vitest` / `tsc` runs. Every claim below
> was either read directly from source or produced by a command that's quoted.
> Where `docs/ANTIGRAVITY_CURRENT_STATE.md` (same-day prior audit by a
> different agent) is used, its load-bearing claims were re-checked against
> the filesystem rather than trusted as-is, per the brief.

## 0. TL;DR for whoever reads this next

- The backend is genuinely mature — real providers, real deterministic
  intelligence, real tests, 309/313 passing. Trust it.
- The frontend **does not currently build**. `npm run build` fails on HEAD
  with 7 TypeScript errors, all introduced by the latest commit. Fix this
  before anything else — see §4.
- README is stale (says "Phase 2," "AI/frontend not yet") exactly as the
  brief warned. Ignore it until rewritten.
- Everything the new 57-section spec asks for around **historical/upcoming
  browsing, routing, session explorer, driver/team/circuit pages, search,
  and circuit maps is greenfield** — none of it exists in the frontend today,
  even though the backend has most of the data plumbing to support it.

---

## 1. Git History

```
ff7c437 (HEAD -> main, origin/main) Implement Phase C: P1 Deep Analysis
        (WhatChanged, PitAnalytics, LapIntelligence, QualifyingCutLine,
        PracticeClassification) and Phase A/B improvements
        — author: Antigravity AI <antigravity@google.com>, 2026-08-24
302cdee (tag: v0.8.0) F1 Intelligence Platform — Phases 0–8
```

Only two commits reach `origin/main` (the earlier phase-by-phase history was
squashed into `302cdee`). Working tree is clean. This repo was built by
Google's Antigravity agent up through yesterday; this is the first Claude
pass on it.

The latest commit touched **29 files, +4834/−761 lines**, almost entirely in
`frontend/` (App.tsx, AIConsole.tsx, Panels.tsx, TelemetryLab.tsx,
TimingTower.tsx, TyreTimeline.tsx, tokens.css, styles.css, store.ts,
format.ts, main.tsx, plus 6 new `components/analysis/*` files) and the Stitch
design reference. **Zero backend logic files changed** (only autogenerated
`egg-info`), and **zero test files changed** — a 4834-line diff landed with
no new or updated tests.

---

## 2. Backend — verified module map (`backend/app/`, 696K)

| Package | Contents (verified via directory listing) |
|---|---|
| `core/` | canonical `models.py` (13 entities), `enums.py`, `events.py` (envelope/bus), `session_state.py`, `source_policy.py` |
| `providers/` | `base.py` (Capabilities/RawItem contract), `openf1/` (52K — largest, matches "production" claim), `signalr/` (32K), `jolpica/` (32K), `fastf1/` (20K), `f1db_provider.py` (single 4K file), `replay.py` |
| `ingest/` | `pipeline.py`, `normalize.py`, `persistence.py`, `quality.py`, `recorder.py`, `corrections.py`, `failover.py` |
| `analysis/` | 24 files, 216K total — see §2a |
| `realtime/` | `hub.py` (SessionHub), `differ.py` (SnapshotDiffer), `sequencer.py`, `telemetry.py` (coalescer), `metrics.py`, `compare.py` |
| `ai/` | `gateway.py`, `providers.py` (Gemini/OpenAI-compatible/OpenRouter/Mock), `jobs.py`, `validation.py` (grounding), `prompts.py`, `change.py` |
| `storage/` | `db.py` (asyncpg pool), `downsample.py` (LTTB), `migrations/` |
| `api/` | single `__init__.py`, 24.6K — see route table in §5 |

**2a. `analysis/` has duplicate v1/v2 modules**: `battles.py` + `battles2.py`,
`strategy.py` + `strategy2.py`, `tyres.py` + `tyres2.py`, plus a standalone
`racepace2.py`. It's not obvious from the filenames alone which is canonical
for new work — the snapshot code (`analysis/__init__.py`) pulls from the "2"
variants for the intelligence pack. This is the same *dual-implementation*
shape that caused the UDPProvider race condition in RaceMind-AI — worth
resolving explicitly (rename/deprecate the losers) before adding a third
generation of any of these.

---

## 3. Frontend — verified module map (`frontend/src/`, 228K)

```
components/
  App.tsx, ai/, analysis/ (6 new Phase-C components), panels/,
  shared/ (Panel, ProvenanceBadge, ConfidenceBadge, TyreChip, TimingValue,
           EvidenceChip, Metric, Delta), shell/, telemetry/, timing/, tyres/
design/tokens.css
logic/format.ts
state/store.ts   (useSyncExternalStore over one SessionSocket)
ws/socket.ts
```

**No `pages/`, `routes/`, `router/`, or comparable directory exists.**
`frontend/package.json` dependencies are exactly `react` + `react-dom`
(runtime) and `vite`/`typescript`/`vitest`/`@vitejs/plugin-react`
(tooling) — **no routing library of any kind is installed.** The app is a
single implicit screen that assumes a session is already selected; there is
no client-side navigation to add a router on top of later, it has to be
introduced from scratch.

---

## 4. Verified test & build status (ran, not assumed)

### Backend
```
cd backend && python3 -m venv .venv && .venv/bin/python -m pip install -e ".[dev]"
```
succeeds, but **is not actually sufficient to run the server or the full
suite**: `app/api/__init__.py` and `scripts/serve_realtime.py` import
`fastapi` (and transitively need `uvicorn`, `websockets`), none of which are
listed in `backend/pyproject.toml`'s `dependencies`. Confirmed by
`pytest --collect-only`: 285/313 tests collect; `tests/test_realtime.py`
fails with `ModuleNotFoundError: No module named 'fastapi'`.

After manually adding `fastapi uvicorn websockets`:
```
1 failed, 309 passed, 3 skipped, 1 warning in 11.4s
```
- **1 real failure** (not environmental): `test_gemini_provider.py::TestRealGeminiAPI::test_real_generate_content`
  calls `self.skipTest(...)`, but the class isn't a `unittest.TestCase`, so the
  intended "skip if no key" path raises `AttributeError` instead of skipping.
  One-line fix: `pytest.skip(...)` or a `skipif` marker.
- **3 skips, all legitimate**: `GEMINI_API_KEY not configured`, `replay
  requires setup`, `mini recording fixture not built` — none are bugs.

**Fix needed:** add `fastapi`, `uvicorn`, `websockets` to
`backend/pyproject.toml` dependencies. A clean clone cannot start the server
or run the full suite today without this.

### Frontend
`npm install` is clean. Existing tests pass and were **not** regressed by
the 4834-line commit:
```
✓ tests/socket.test.ts (10 tests)
✓ tests/core.test.ts (7 tests)
Test Files  2 passed | Tests 17 passed
```
But **`npm run build` (`tsc && vite build`) fails on HEAD** — 7 TypeScript
errors, all in the 4 components the latest commit added:

- `LapIntelligence.tsx`, `PitAnalytics.tsx`, `PracticeClassification.tsx`,
  `QualifyingCutLine.tsx` all call
  `<ProvenanceBadge level="HIGH" label="..." />`, but `ProvenanceBadge`
  (`shared/index.tsx:68`) only accepts `type: "LIVE"|"DERIVED"|"ESTIMATED"|"HYPOTHETICAL"|"AI"`.
  This looks like a mix-up with the separate `ConfidenceBadge` component,
  which is presumably the `level`/`label`-shaped one.
- `LapIntelligence.tsx` also calls `sectorStyle(l.s1_pb, l.s1_ob)` with two
  arguments against a one-argument signature in `logic/format.ts`.
- None of the 6 new Phase-C components (the above 4, plus `TimeDelta.tsx` and
  `WhatChanged.tsx`) have any test coverage.

**Practical effect: the app cannot currently pass its own documented build
command.** This should be the first fix made, ahead of any new-spec work —
building further UI on top of a non-compiling base compounds the problem.

---

## 5. Existing backend APIs / routes (unchanged by latest commit — `git show --stat` confirms `api/__init__.py` untouched)

| Route | Method | Purpose |
|---|---|---|
| `/api/v1/health` | GET | health check |
| `/api/v1/live-data-status` | GET | provider latency + quality |
| `/api/v1/sessions` | GET | active sessions |
| `/api/v1/sessions/{id}/snapshot` \| `/drivers` \| `/leaderboard` \| `/events` | GET | core session views |
| `/api/v1/sessions/{id}/sectors/{drv}` \| `/tyres/{drv}` \| `/pace/{drv}` | GET | per-driver intelligence |
| `/api/v1/sessions/{id}/telemetry/{drv}` \| `/telemetry/compare` | GET | LTTB-downsampled traces |
| `/api/v1/sessions/{id}/intelligence` | GET | full intelligence pack |
| `/api/v1/sessions/{id}/strategy/candidates` | GET | strategy candidates |
| `/api/v1/sessions/{id}/contextpack` \| `/ai/ask` \| `/api/v1/ai/jobs/{id}` \| `/api/v1/ai/status` | GET/POST | AI layer |
| `/api/v1/sessions/{id}/circuit` | GET | **always returns `available: false`** — no geometry source wired |
| `/api/v1/replay/{id}/control` | POST | replay transport (play/pause/speed) |
| `/ws/session/{id}` | WS | snapshot/delta/events/telemetry/AI stream |

**Frontend routes: none.** No client-side routing exists at all (§3).

---

## 6. Data providers — verified capability reality

| Provider | Status | Notes |
|---|---|---|
| OpenF1 | Production | live polling + historical backfill; largest provider module (52K) |
| F1 SignalR | Implemented, disabled by default | direct feed abstraction; token-less negotiate/snapshot verified 2026-08-24 per code comments — not re-verified live here (network-restricted sandbox, see §8) |
| FastF1 | Adapter ready | historical/high-res, class B provenance |
| Jolpica | Implemented | schedule/results/standings |
| F1DB | **Deliberately stubbed** | `f1db_provider.py` returns empty everywhere; in-code comment confirms `f1db/f1db` v2026.12.0 was verified active on 2026-08-23 and the integration was consciously deferred rather than faked — this is exactly the honest "nothing claimed" pattern the ground rules call for, and it's the natural next build |
| Replay | Production | recordings via `.jsonl.zst`, `ReplayProvider` |

Circuit geometry (spec §20/§35) is the one concrete blocker for the MAP
workspace: the endpoint exists and honestly reports unavailable. F1DB is the
identified-but-unbuilt path to fill it.

---

## 7. Known UX limitations (frontend)

`docs/ANTIGRAVITY_CURRENT_STATE.md` (generated the same day as HEAD, just
before the last commit) listed 17 UX limitations. The last commit rewrote
`App.tsx`, `TimingTower.tsx`, `AIConsole.tsx`, `Panels.tsx`,
`TelemetryLab.tsx`, and `TyreTimeline.tsx` substantially, so some of these are
plausibly improved — **but I have not individually re-verified each item
against the diff**, and I'd rather say that than guess. Treat that list as
"true as of yesterday, needs a fast re-check" rather than either fixed or
still-broken. Recommend that re-check as an early P0/P1 task before assuming
any of it is resolved. The ones I can independently confirm are still true
right now, because they concern things that didn't exist before or after this
diff:
- No circuit map / geometry (confirmed §6).
- No replay transport UI (backend supports it; no frontend controls found in
  the component tree).
- No responsive/tablet/mobile layout handling found.
- No routing, session browser, or history pages of any kind (§3) — this is
  new information beyond the prior audit, since it's about the *new* spec's
  requirements rather than the existing product.

---

## 8. Environment constraints on this audit itself

- This sandbox can reach GitHub, PyPI, and npm (which is how the clone and
  installs above worked) but **not** OpenF1, F1 SignalR, FastF1's backing
  data source, or Jolpica's API — those hosts aren't on its network
  allowlist. So provider *code* was inspected and its *declared* capabilities
  recorded, but no live provider call was exercised here. Live verification
  (`scripts/live_acceptance.py`, `scripts/probe_signalr.py`, etc.) needs to
  run somewhere with real network access to those hosts.
- No push credentials to `T-Bone47/live-f1-intelligence` are available here —
  this file and any subsequent code changes are produced as files, not
  commits, until applied from the real machine or from an environment with
  write access.

---

## 9. What the new 57-section spec already has vs. what's greenfield

**Already substantially covered (backend-side; frontend consumption varies):**
- Provider abstraction, canonical schemas, provenance, dedupe, persistence,
  recording/replay — spec §1, §4–10.
- Deterministic intelligence (timing/sectors/pace/tyres/battles/strategy/
  weather/RC/qualifying/practice/confidence) — spec §19–26, §29–32 backend
  logic exists; frontend only partially consumes it (§7 of the prior audit).
- Realtime WS with sequencing/resume — spec §27–28's state-sharing
  requirement has real backend plumbing to build on.
- AI Gateway with grounding/evidence — spec §25, §31.

**Confirmed greenfield (nothing in the frontend today):**
- Any routing (spec §38) — zero router dependency installed.
- LIVE/UPCOMING/HISTORICAL/REPLAY home state machine (spec §13–15) — today's
  app assumes a session is already selected; no "no live session, show
  latest completed" fallback exists.
- Session Explorer, driver/team/circuit pages, cross-race comparison, global
  search (spec §16, §33–37) — no components, no backend endpoints for
  season/event discovery outside a single active session.
- Circuit geometry / MAP workspace (spec §20) — endpoint exists, returns
  nothing; F1DB provider is a deliberate stub.
- Canonical session index / data coverage matrix (spec §11–12, §39).

---

## 10. Recommended immediate next steps (before any new-spec P0 work)

1. Add `fastapi`, `uvicorn`, `websockets` to `backend/pyproject.toml` — 2 min,
   unblocks a clean clone from running the server or full test suite at all.
2. Fix the 7 TS build errors (`ProvenanceBadge`/`ConfidenceBadge` prop
   mix-up in 4 files + `sectorStyle` arity in 1) — restores `npm run build`.
3. Fix `test_gemini_provider.py`'s broken `skipTest` call.
4. Re-verify the 17-item UX-limitation list against the current diff,
   item by item, rather than assuming Phase A/B fixed them.
5. Only then start the new spec's P0 (provider priority matrix doc, canonical
   session index, Jolpica-backed historical browsing UI — Jolpica is already
   "implemented" backend-side, so this is the cheapest path to a first
   working Session Explorer).

**Update 2026-08-25, same day, follow-up commits `f71bb7f`–`b39e42a`:**
Steps 1–3 above are done, plus one more found while fixing #2:

- `f71bb7f` — added `fastapi`/`uvicorn[standard]` to `backend/pyproject.toml`.
- `057d9d9` — `test_gemini_provider.py` now uses `pytest.skip`. Full backend
  suite: **309 passed, 4 skipped, 0 failed.**
- `8269b35` — fixed the `ProvenanceBadge`→`ConfidenceBadge` prop mismatch in
  `PitAnalytics`, `PracticeClassification`, `QualifyingCutLine` (added an
  optional `label` to `ConfidenceBadge` rather than stretching
  `ProvenanceBadge`'s closed `type` union to fit a source name).
- `b39e42a` — **`LapIntelligence` was worse than a type error**: it spread
  `usePace()`'s return value (a single rolling-average object) as if it were
  an array of laps, then read fields (`lap_number`, `s1_pb`, `s1_ob`, ...)
  that don't exist anywhere in the backend. That throws at runtime the
  instant a driver is selected — `tsc` missed it because `pace` is typed
  `any`. No endpoint currently returns per-lap sector history, so this
  isn't a quick data-shape fix; the panel now honestly says "not available"
  instead of crashing or fabricating rows. A real fix needs a new backend
  endpoint (recent laps with S1/S2/S3 + classification) — that's genuine
  P0/P1 feature work, not a mechanical error.

`npm run build` and the full backend suite are both green as of `b39e42a`.
Item 4 (re-verifying the 17-item UX list) is still open, and repo-wide ruff
found ~293 pre-existing style issues outside anything touched here —
noted, not fixed, out of scope for this pass.

**Update 2026-08-25, later same day, commits `75b1ecc`–`20230e1`:**
Two more real bugs, found while checking whether the app is actually
ready to run (it wasn't — see below):

- `75b1ecc` — `TimeDelta`/`TheoreticalLap` read `sectorsA.sectors[...]` and
  `sectors.personal_best_lap_s`; the real `/sectors/{driver}` response has
  `personal_best`/`last` (flat numbers keyed `S1`/`S2`/`S3`) and no
  `personal_best_lap_s` at all. Fixed to read the real fields; pulled
  "actual best lap" from the leaderboard row's `personal_best_s` instead
  (matches `TimingTower`'s own usage). Dropped `aCls`/`bCls` — classification
  isn't available from this endpoint and those values were never rendered
  anyway.
- `20230e1` — `serve_realtime.py`'s shutdown path referenced `ai_runtime`,
  a variable local to a different function (`NameError` on clean shutdown,
  not a startup blocker). Now returned from `build_runtime()` and unpacked
  in `amain()`.

**Update 2026-08-25, later still, commits `f4aa28e`–`7bfb2c2`:** fixed the
`TimingTower` finding above rather than leaving it open.

`SectorEngine` (`backend/app/analysis/sectors.py`) already tracks everything
needed as live state — `personal_bests()` and `session_best_holders()` — it
just wasn't exposed. Added `classification` to `/sectors/{driver}`: a
driver's PB for a sector is, by construction, either the current session
record (PURPLE) or not (GREEN). YELLOW is correctly never emitted here —
it describes a specific crossing being slower than the PB *at that moment*,
which isn't a meaningful "current status" for a PB summary; it only made
sense as a per-crossing event, which is a different feature than this
endpoint. Purely additive — `personal_best`/`last`/`theoretical_lap_s`/
`available` unchanged, so the `TimeDelta` fix from earlier isn't affected.
Added `test_sectors_classification_purple_green` (two drivers cross S1,
faster reads PURPLE, slower reads GREEN) — backend suite is now
**310 passed, 4 skipped, 0 failed**.

`TimingTower.tsx` updated to read the real fields
(`d.personal_best.S1`, `d.classification.S1`) instead of the `d.sectors[...]`
key that never existed. `npm run build` and `npm test` (17/17) both still
green.

**Run-readiness, checked directly rather than assumed:** Postgres is not a
hard boot blocker — `db_connect()` is called lazily inside
`/telemetry/{driver}` only, not at app startup, and `create_app()` is
proven safe to construct/serve in-process (`test_realtime.py` does exactly
that via `TestClient`). What *is* blocking a real end-to-end run: no
`recordings/` directory exists (gitignored by design — a session has to be
recorded from a live feed first) and this sandbox has no network path to
OpenF1/SignalR/Jolpica/FastF1's sources, so neither `--mode replay` nor
`--mode live` has been launched or confirmed to actually boot and serve.
That test needs to happen somewhere with Docker + real OpenF1 network
access.
